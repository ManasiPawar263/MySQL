# create new database
create database excelr;

# See database created
show databases;

# Use the database as default schema
use excelr;

# create table with column as id, name and age
create table personal_info(
id int,
emp_name char(20),
age int);
